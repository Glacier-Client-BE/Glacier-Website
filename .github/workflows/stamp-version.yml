name: Stamp Asset Versions

# Cache-busts every same-origin JS module import, the entry script/
# stylesheet, and the <meta name="site-build"> marker on every push to
# main — see .github/scripts/stamp-version.js for why this needs to cover
# the whole module graph, not just js/main.js's own URL. Then rebuilds the
# js/dist/app.min.js bundle that index.html actually loads.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: stamp-version
  cancel-in-progress: false

jobs:
  stamp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Bump the cache-busting version
        run: |
          set -euo pipefail
          VERSION=$(date -u +%Y%m%d%H%M%S) node .github/scripts/stamp-version.js

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # index.html ships the single bundle in js/dist/, not the source modules,
      # so rebuild it from the (just-stamped) sources on every push.
      - name: Build the JS bundle
        run: |
          set -euo pipefail
          npm ci --no-audit --no-fund
          npm run build

      - name: Commit and push
        run: |
          set -euo pipefail
          git add index.html 404.html js config/index.html
          if git diff --cached --quiet; then
            echo "No version bump needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: bump asset cache-busting version"

          # Rebase on top of anything pushed since checkout, then push (retry once).
          git push || {
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push
          }
